# Landscape Classifier

Scene recognition model using ResNet50 transfer learning.

## Quick Start

1. Install dependencies: `pip install -r requirements.txt`
2. Download dataset: `python src/utils/kaggle_dataset_installer.py`
3. Open and run: `src/notebooks/landscape_classifier_model.ipynb`

See the root `README.md` for full documentation.
